# ─────────────────────────────────────────────────────────────────────────────
# VL Automobiles — Variables d'environnement
# Copier ce fichier en .env.local et renseigner les valeurs
# NE JAMAIS committer .env.local dans Git
# ─────────────────────────────────────────────────────────────────────────────

# ── Base de données Supabase ──────────────────────────────────────────────────
# Récupérer depuis : Supabase > Settings > Database > Connection string

# URL avec pooling PgBouncer (pour les requêtes en production)
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-eu-west-3.pooler.supabase.com:6543/postgres?pgbouncer=true"

# URL directe (pour les migrations Prisma)
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-eu-west-3.pooler.supabase.com:5432/postgres"

# ── NextAuth.js ───────────────────────────────────────────────────────────────
# Générer avec : openssl rand -base64 32
NEXTAUTH_SECRET="REMPLACER_PAR_UNE_VALEUR_ALEATOIRE_LONGUE"
NEXTAUTH_URL="http://localhost:3000"
# En production : NEXTAUTH_URL="https://votre-domaine.com"

# ── 2FA — Chiffrement du secret TOTP ─────────────────────────────────────────
# Générer avec : openssl rand -base64 32
TOTP_ENCRYPTION_KEY="REMPLACER_PAR_UNE_CLE_AES_256"

# ── Stockage images ───────────────────────────────────────────────────────────

# Option A : Cloudinary (recommandé pour transformations avancées)
CLOUDINARY_CLOUD_NAME="votre-cloud-name"
CLOUDINARY_API_KEY="votre-api-key"
CLOUDINARY_API_SECRET="votre-api-secret"

# Option B : Vercel Blob (plus simple si déploiement sur Vercel)
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# ── Notifications email ───────────────────────────────────────────────────────
# Resend (recommandé — simple, fiable, 100 emails/jour gratuit)
RESEND_API_KEY="re_..."
RESEND_FROM_EMAIL="noreply@vl-automobiles.fr"

# Email de l'admin qui reçoit les notifications de nouveaux leads
ADMIN_NOTIFICATION_EMAIL="contact@vl-automobiles.fr"

# ── URL publique du site ──────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# En production : NEXT_PUBLIC_APP_URL="https://votre-domaine.com"


# ═══════════════════════════════════════════════════════════════
# CLOUDINARY (Upload d'images) - Étape 6
# ═══════════════════════════════════════════════════════════════

CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"